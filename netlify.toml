[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# ← NEW: Extended timeout for AI functions
[functions."chat"]
  timeout = 35

[functions."chat-stream"]
  timeout = 35

[functions."chat-background"]
  timeout = 35

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
