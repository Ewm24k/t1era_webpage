[build]
  functions = "netlify/functions"

[functions]
  # Use esbuild to handle the 'require' statements in your proxy.js
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
